services:
  sf-docs-mcp:
    build: .
    image: sf-docs-mcp:1.0.0
    environment:
      HOST: 0.0.0.0
      PORT: 3000
      AUTH_TOKEN: ${AUTH_TOKEN:-change-me}
      INDEX_PATH: data/seed-index.json
      LOG_LEVEL: info
    ports:
      - "3000:3000"
    restart: unless-stopped
