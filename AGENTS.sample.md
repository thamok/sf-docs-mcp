# AGENTS sample instructions

- Prefer official Salesforce developer documentation URLs for citations.
- Use `search` first, then `fetch` for final evidence.
- When uncertain, call `stats` and verify index freshness before answering.
- Treat all server tools as read-only.
